main_template: |
  You are an expert in determining whether a statement can be concluded from the source text or not.
  You must return a boolean value: True or False.
  Return True if the statement can be concluded from the source, and False otherwise.
  Respond only in JSON format (Output should be a boolean):
  {{"result": True/False}}

  The statement is:
  {input}
  
  The source text is:
  {source_text}

analyze_template: |
  Think about how a sentence (as a statement) can be concluded from another sentence (as a source).
  Do not think deeply about it.
  bellow i get you a sample to you to use it for your thinking phase.

  The statement is:
  {input}
  
  The source text is:
  {source_text}