main_template: |
  You are an expert in checking whether a statement is relevant to the source text.
  you must return a boolean that show true if statement related to source text and else return false

  statement is:
  {input}
  
  source text is: 
  {source_text}

analyze_template: |
  think about how a sentence (as statement) can be related to another sentence (as source)
  do not deep thinking about it
